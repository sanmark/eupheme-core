version: '3'
services:
  lk-sanmark-minerva-dev:
    build:
      context: .
      dockerfile: dev.Dockerfile
    env_file:
      - ./src/.env
    image: lk-sanmark-minerva-dev
    ports:
      - 80:80
    volumes:
      - ./src:/app
